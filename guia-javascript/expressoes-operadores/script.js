/*Expressões e operadores

- Expressions
- Operators
    Binary
    Unary
    Ternary

-> New:
    *left-hand-side expression
    *criar um novo objeto


-> Operadores logicos:
    * AND (&&) - TRUE - TRUE

    * OR || TRUE - FALSE

    * NOT !

-> Operador ternário:
  
  //condição então valor 1 se não valor 2
  //condition ? value1 : value2
  //condition(TRUE) => value1
  //condition(FALSE) => value2

  let pao = true
  let queijo = true

  const niceBreakfast = pao && queijo ? 'café top' : 'Café normal'

-> Operator precedence
    *grouping                ()
    *negação e incremento    ! ++ --
    *multiplicação e divisão * /
    *adição e subtração      + -
    *relacional              < <= > >= 
    *igualdade               == != === !==
    *AND                     &&
    *OR                      ||
    *CONDICIONAL             ?:
    *Assignment (atribuição) = += -= *=
*/

